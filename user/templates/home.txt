<div class="card">
  <div class="table-responsive">
    <table class="table align-items-center mb-0">
      <thead>
        <tr>
          <th class="text-uppercase text-dark text-xs font-weight-bolder opacity-9">Nome</th>
          <th class="text-uppercase text-dark text-xs font-weight-bolder opacity-9">Email</th>
          <th class="text-uppercase text-dark text-xs font-weight-bolder opacity-9 ps-2">Função</th>
          <th class="text-center text-uppercase text-dark text-xs font-weight-bolder opacity-9">Início</th>
          <th class="text-secondary opacity-9"></th>
        </tr>
      </thead>
      <tbody>
        {% for member in membership %}
        <tr>
          <td>
            <div class="d-flex px-2 py-1">
              <div class="d-flex flex-column justify-content-center">
                <h6 class="mb-0 text-sm text-dark">{{ member.user.name_user }}</h6>
              </div>
            </div>
          </td>
          <td>
            <div class="d-flex px-2 py-1">
              <div class="d-flex flex-column justify-content-center">
                <p class="text-sm mb-0 text-dark">{{ member.user.email }}</p>
              </div>
            </div>
          </td>
          <td>
            {% if member.role == "owner" %}
              <div class="role owner px-2 py-1 rounded text-center text-xs fw-bold">Proprietário</div>
            {% elif member.role == "admin" %}
              <div class="role admin px-2 py-1 rounded text-center text-xs fw-bold">Administrador</div>
            {% else %}
              <div class="role member px-2 py-1 rounded text-center text-xs fw-bold">Membro</div>
            {% endif %}
          </td>
          <td class="align-middle text-center">
            <span class="text-dark text-sm fw-semibold">
              {{ member.user.date_joined|date:"d M Y" }}
            </span>
          </td>
          <td class="align-middle">
            <a href="javascript:;" class="text-primary fw-bold text-sm" data-toggle="tooltip" title="Editar usuário">
              Editar
            </a>
          </td>
        </tr>
        {% empty %}
        <tr>
          <td colspan="5" class="text-center text-muted">Nenhum membro encontrado.</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
