{% extends 'base.html' %}
{% load static %}

{% block title %}{{ request.user.org_active.name_org }}{% endblock %}
{% block content %}
<link rel="stylesheet" href="{% static 'details_org.css' %}">
<div class="header_details_org">
  <h3 class="title_details_org">{{ request.user.org_active.name_org }}</h3>
  
    {% if view.get_role == "owner" %}
    <div class="role owner">Proprietário</div>
    {% elif view.get_role == "admin" %}
    <div class="role admin">Administrador</div>
    {% else %}
    <div class="role member">Membro</div>
  {% endif %}
</div>

{% if view.get_role == "owner" or view.get_role == "admin" %}
<!-- Arquivo CSS não carrega -->
<div style="margin: 1.5% 0%;">
  {% if view.get_role == "owner"%}
  <button type="button" class="btn bg-gradient-info btn_staff" style="margin: 0% 0.5% 0% 0%;">Editar Organização</button>
  <!-- modal com para editar name_org e is_active 
       Talvez usar HTMX para ataulizar (acho que não precisa) -->
   
  {% endif %}
  <button type="button" class="btn bg-gradient-info btn_staff" style="margin: 0% 0.5% 0% 0%;">Minhas Tarefas</button>
  <button type="button" class="btn bg-gradient-info btn_staff" style="margin: 0%;">Membros da Organização</button>
</div>

<div>
{% for member in membership %}
  {{ member.user.name_user }} - {{ member.role }} - {{ member.user.date_joined }}
{% endfor %}
</div>

{% else %}
AQUI é pra membro
{% endif %}
{% endblock %}